on:
  push:
    branches:
      - development
      - principios
name: 🚀 Deploy dev
jobs:
  build:
    name: 🌐 Deploy Back
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Create back/database.ini file
        env:
          DBServ: localhost
          DBConn: mysql
          DBName: ${{ secrets.DBNameDev }}
          DBUser: ${{ secrets.DBUserDev }}
          DBPass: ${{ secrets.DBPassDev }}
        run: |
          touch back/database.ini
          echo DBServ	= $DBServ >> back/database.ini
          echo DBConn	= $DBConn >> back/database.ini
          echo DBName	= $DBName >> back/database.ini
          echo DBUser	= $DBUser >> back/database.ini
          echo DBPass	= $DBPass >> back/database.ini

          echo "cat back/database.ini"
          cat back/database.ini

      - run: ls -l
      - run: ls -l back

      - name: Delete file
        run: rm -f back/db/database.ini

      - run: ls -l
      - run: ls -l back

      - run: ls -l
      - run: ls -l back

      - name: 🎉 Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ftp.casaloca.es
          username: ${{ secrets.ftp_username }}
          password: ${{ secrets.ftp_password }}
          local-dir: ./back/
          server-dir: /tfg_dev/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/vendor/**
